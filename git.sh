#!/bin/bash
sudo git add .  || git commit -m "update"
sudo git commit -m "update" || git commit -m "update" -a
sudo git pull || git push
sudo git push || git pull
sudo git add .    || git pull
sudo git pull || git add .
sudo git pull || git commit -m "update" -a
sudo git add . || git commit -m "update" -a
sudo git commit -m "update" -a  || git add .